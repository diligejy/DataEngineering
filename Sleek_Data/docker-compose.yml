version: '3'

services:
  diligejy-airflow:
    image: diligejy-airflow:latest

    volumes:
      - ./airflow:/opt/airflow 

    ports:
      - "8080:8080"
    
    command: airflow standalone

  diligejy-smtp:
    image: bytemark/smtp
    restart: always